# --- OCaml / Dune ---
_build/
_opam/
*.install
*.byte
*.native
.merlin

# --- Coverage ---
_coverage/
bisect*.coverage

# --- Node / Vite ---
node_modules/
web/ui/node_modules/
web/ui/dist/
web/ui/.vite/
yacewo-worker/node_modules/
yacewo-worker/.wrangler/
yacewo-worker/.dev.vars

*.local

# Generated engine copy for local Vite (rebuilt by `make web-engine`).
web/ui/public/yacewo_engine.js

# GitHub Pages output from `make web` / CI. Deployed by Actions — do not commit.
docs/

# --- Secrets / credentials (never commit) ---
.env
.env.*
!.env.example
*.pem
*.p12
*credentials*
*secret*
auth.json

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
*.swp
*~
.cursor/

# --- Local research / agent dumps ---
frost-glass-backgrounds.json
*.search.json
